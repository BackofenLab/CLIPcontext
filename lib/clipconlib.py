#!/usr/bin/env python3

import sys
import re
import statistics
import os

"""
Mean stdev.
set_avg = statistics.mean(fvl)
set_stdev = statistics.stdev(fvl)


The class label can later be 

It later on can be used to decide for its class label given a threshold.

"""
